# breakfast

## Start in local

`docker-compose up -d`

`symfony server:start -d`

`symfony open:local`

## If you want to have some data
`make fixture`

## Test unitaire
`make test`

## phpstan
`make phpstan`
